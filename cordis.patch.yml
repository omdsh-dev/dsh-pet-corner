# Profile Bundle entry. dsh plugin add applies this row once and discovers the
# browser half through package.json#dsh.client.
- insert:
    - id: pet-corner
      name: '@deepseek-ai/dsh-pet-corner'
